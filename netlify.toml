[build]
  command = "npm install --legacy-peer-deps && npx ng build --configuration=production --verbose"
  commandOrigin= "config"
  publish= "dist/creditos-front-end/browser"
  publishOrigin= "ui"
functionsDirectory= "/opt/build/repo"
